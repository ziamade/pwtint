---
/**
 * Features component is intentionally empty - the "Advanced Tinting Solutions"
 * heading is part of the Services component, and the feature list / stats
 * are in the Stats (Why Choose) component. This matches the original site
 * where these are combined sections, not separate ones.
 */
---
